# TODO: set from .env but I don't plan on using it so no need

RQ_QUEUES = {
    "default": {
        "HOST": "localhost",
        "PORT": 6379,
        "DB": 0,
        "USERNAME": "some-user",
        "PASSWORD": "some-password",
        "DEFAULT_TIMEOUT": 360,
    },
}
